# Task 1 Report — Аудит-реестр решений (оптимизация CSS/SVG)

Дата: 2026-08-24. Ветка: `refactor/css-cleanup`. Коммит: `8f8828f` «docs: optimization audit registry (sprite/dupes/vendors)».

## Что произведено

Деливерабл: `docs/superpowers/plans/2026-08-24-optimization-audit.md` — три реестра:

### РЕЕСТР-A «Спрайт»
- Скан всех 15 HTML: пути `d="…"` ≥40 символов, группировка по первым 80 символам → 49 уникальных ключей, **27 повторяющихся групп, 439 повторяющихся инстансов**.
- 12 целевых символов с ролевыми id: `icon-arrow-card` (96 инстансов — 4 варианта экспорта одного глифа, визуальная идентичность подтверждена рендером в браузере), `logo-mark` (30), `logo-wordmark` (150, 10 путей одного хедер-логотипа), `icon-search` (15), `icon-rutube` (49, 3 пути одного SVG), `icon-vk` (17), `icon-quote` (17), `icon-play` (3), `icon-download` (2).
- 60 инстансов — уже существующие `<symbol>`-определения (`icon-arrow/right/left/caret`) — помечены «не трогать». **Зона замены: 379 инстансов / 23 группы.**
- Контекст каждой группы проверен по разметке (классы-обёртки, aria-label); направления стрелок определены рендером, а не догадкой.
- Экспорт для Task 2: `C:\Temp\opencode\sprite-inventory.xml` (CliXML, `{Id, Pages, Count, PathPrefix}`), PathPrefix — сырой текст HTML (без нормализации пробелов) для точного текстового поиска. Round-trip проверен Import-Clixml.

### РЕЕСТР-B «Дубли деклараций»
- Полный перенос всех **71 группы** из `css-rules.xml` (825 правил вне @media): count, все члены (файл+селектор), нормализованное тело.
- Разметка merge/skip: **merge 61 группа (160 лишних инстансов)** — бренд-hover (G02×18, G04×12, G05×9), hidden-state (G03×17), ритм секций (G01×20, G10, G36, G63), swiper-overflow (G06×6), типовые цвета, панели/гриды/аккордеоны, близнецы страниц solution-detail↔univer-online (G31/32/37).
- **skip 10 групп (14 инстансов)**: G15, G20, G23, G28, G50, G51, G52 (кадры @keyframes — вообще не дубли правил), G54, G59, G66 — случайные совпадения тел у разных по смыслу селекторов.
- Находка попутно: G64 — захардкоженный `background: #1342f3` (заменить на `var(--color-sea)` при merge).

### РЕЕСТР-C «Вендоры»
Матрица по 15 страницам (Swiper css / использование разметки / Fancybox css+js / data-fancybox / .video / .gallery). Все 4 ожидания плана подтверждены измерениями:
- Swiper не используется (0 токенов swiper-разметки, включая `__swiper`/`swiper-wrapper`/`swiper-slide`) на **contacts, event-detail, solutions** (+ projects уже чист) → снять.
- Fancybox на **contacts** подключён (css+js) при нуле маркеров → снять.
- **solutions.html**: осиротевший `data-fancybox`+`data-src="#request-popup"` на кнопке при отсутствии библиотеки → удалить атрибуты.
- Доп. находка вне скоупа: **vuz.html** подключает `fancybox.umd.js` без css и без какого-либо использования — кандидат на снятие в отдельной задаче (в матрице помечен ⚠).
- Безопасность снятия подтверждена по `js/main.js`: `initSwiper` не вызывает `new Swiper` при пустом NodeList; Fancybox-ветки защищены `typeof Fancybox === 'undefined'`.

## Отклонения от ожиданий
- План ожидал ≈426 повторяющихся SVG-инстансов; измерено **439** (в зоне замены 379). Суммы сходятся до инстанса; расхождение — методика оценки плана (границы длины пути/учёт symbol-defs). На решения Task 2 не влияет — в реестре разложение по группам полное.
- 71/174 по CSS воспроизведены точно.

## Файлы
- Создан: `docs/superpowers/plans/2026-08-24-optimization-audit.md` (единственный коммит, 8f8828f).
- Служебные (вне репо): `C:\Temp\opencode\sprite-inventory.xml` (потребляет Task 2), `css-rules.xml`, `dupes-full.txt`, `svg-inventory.txt`, `vendors.txt`, скрипты `task1-*.ps1`.

## Самопроверка
- A: 27 групп → сумма 439 ✓; replaceable 23 группы → 379 ✓; XML round-trip ✓; у каждого символа перечислены страницы и ключи.
- B: 71 группа = 61 merge + 10 skip ✓; 160+14 = 174 лишних инстанса ✓; члены merge-групп перечислены полностью.
- C: 15 страниц в матрице; 4 ожидания подтверждены; вердикт на каждую страницу.
- Производственный код не менялся; в коммит вошёл только деливерабл.
