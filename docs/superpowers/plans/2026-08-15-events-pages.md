# План: страницы «Медиацентр» (listing) и «Детальная мероприятия» (2026-08-15)

Две подстраницы по макету «SoftSnow Work (Copy) (Copy)» (fileKey `fpzIQVun5Ql9sicHmL2JQ2`), SECTION node `895:7964` (9223×10055):

- **`events.html`** — «Медиацентр», node `895:9289` (frame 1920×4048).
- **`event-detail.html`** — «Детальная мероприятия», node `896:9497` (frame 1920×7991).

Пользователь подтвердил: строим **обе** страницы (по образцу clients/client-detail). Многостраничный паттерн: все страницы в корне, пути `css/...`, `assets/img/...` без `../` (страниц в `pages/` НЕТ — только корневые `*.html`).

## Статус этапов

| # | Этап | Статус |
|---|------|--------|
| 0 | FIGMA_AUDIT (listing `895:9289` + detail `896:9497`) | [x] |
| 1 | План (этот файл) | [x] |
| 2 | IMPLEMENT: `events.html` (listing) | [x] |
| 3 | IMPLEMENT: `event-detail.html` (detail) | [x] |
| 4 | FIGMA_QA + скриншоты + сверка | [x] |
| 5 | Обновить `FIGMA.md` | [x] |

## Результат аудита (mapping)

### Listing — `events.html` (Медиацентр)

| # | Figma Section | Figma node | Code | Решение |
|---|---|---|---|---|
| 1 | Header INSTANCE `895:9301` | — | `.header` | reuse (копия) |
| 2 | Hero (Frame 2094, 1870×607, bg `#f5f4f5`, r20): breadcrumb, h1 «Медиацентр» 100/500, subtitle 652px, кнопка «Подписаться на рассылку» 256×64 r50, соцсети Rutube/VK | `895:9292` | `.page-hero` + модификатор `.page-hero--media` | NEW modifier (кнопка + соцсети справа) |
| 3 | «Не пропустите ближайшие мероприятия» 70/500 + 2 wide-карточки события 890×560 IMAGE r20, оверлей 450×142 (title 20/500, tag+date 12, arrow) | `895:9316`/`895:9318` | `.events` + `.events__card--wide` | **reuse** (компонент главной!) |
| 4 | Tabs (Все/Мероприятия/Статьи/Новости) + сетка 6 карточек 460×560 IMAGE r20, оверлей 440×140 + «Показать ещё» 460×64 r50 | `895:9335`/`895:9336` | `.tabs` + `.events__card` + `.btn--outline` | reuse |
| 5 | CTA (1870×560, bg `#f5f5f5` + image bg): «Нужна консультация по проекту?» 70/500, текст 14/400, кнопка 460×64 `#1342f3` | `895:9413` | `.cta` | reuse |
| 6 | Footer (1870×520) | `895:9420` | `.footer` | reuse (копия) |

### Detail — `event-detail.html` (Детальная мероприятия)

| # | Figma Section | Figma node | Code | Решение |
|---|---|---|---|---|
| 1 | Header INSTANCE `896:9508` | — | `.header` | reuse (копия) |
| 2 | Hero (Frame 2094, 1870×1017, bg `#faf9fa`): IMAGE bg 1870×758, h1 100/500 «Стратегический рывок…», кнопка «Зарегистрироваться» 216×64 r50, Line 50/51, 2 текста снизу 16/500 a.7 | `896:9498` | `.event-hero` | NEW section |
| 3 | «Что обсудим на семинаре» (white r20, 360h): title 70/500, text 20/400, дата «22 апреля 2026» 40/500 `#1342f3`, «11:00–12:00 (мск), онлайн» 20/500 | `896:9551` | `.event-about` | NEW section |
| 4 | «Будет полезно для» (1920×691): title 70/500 + 4 карточки 460×460 (Ректоров/Проректоров/Руководителей 40/500+16/400 a.7; 4-я «Принять участие» `#1342f3`) | `896:9593` | `.audience` | reuse (+модификатор на 4-ю карточку) |
| 5 | «Программа семинара» (1910×791): title 70/500 + слева 4 пункта (круг 64×64 r100, цифра 20/500; 2-й активный `#1342f3` bg+white) + справа IMAGE 925×560 r20 («Блок 1», title 40/500, text 16/400, спикер: фото 130×130 r100 + имя 20/500 + роль 16/400 + «Спикер» 14/400) | `896:9608` | `.program` | NEW section |
| 6 | CTA «Забронируйте место» (1870×363, bg `#1342f3`, r20): title 70/500 white, text 14/400, btn 460×64 r50 | `896:9557` | `.cta--blue` | reuse |
| 7 | «Что разберём на семинаре» (white r20, 1031h): title 70/500 + 6 карточек 590×360 (title 40/500, text 16/400 a.7, `/ 01`–`/ 06` 14/400 a.7) | `896:9564` | `.seminar-topics` | NEW section |
| 8 | «Партнеры мероприятия» (1870×851): title 70/500 + 3 карточки 617×510 (Directum white+лого 130×130 r65 `#ff8600`; Битрикс24 `#1342f3`; Univer Online white+лого IMAGE) | `896:9643` | `.event-partners` | NEW section (вариант `.partners__card`) |
| 9 | «Почему нам доверяют ВУЗЫ?» (white r20, 668h): title 70/500 + 3 блока (иконка 40×40 + title 40/500 + text 16/400) | `896:9667` | `.trust` | NEW section |
| 10 | «С нами работают» (1920×586): title 40/500 + круги-логотипы 259×256 | `896:9715` | `.clients` | reuse |
| 11 | FAQ (1790×632): 4 вопроса 40/500 + ответ 16/400 a.7 + стрелка | `896:9693` | `.faq` | reuse (`.faq__list`) |
| 12 | Footer (1870×520) | `896:9509` | `.footer` | reuse (копия) |

## Переиспользование иконок

- Стрелки карточек/ссылок: SVG-sprite `#icon-arrow` (17×14). Слайдеров нет.
- Rutube/VK иконки: inline-SVG по `vectorPaths` (как в `.footer`/hero главной — скопировать).
- Иконки секции «Почему нам доверяют ВУЗЫ?»: inline-SVG из `vectorPaths` (famicons:school-outline, ph:stack-light, mingcute:safety-certificate-line).
- FAQ-стрелка: `#icon-arrow` (rotate или отдельный вектор «M 16 8 L 8 0 L 0 8» → `#icon-arrow-down`).

## Новые файлы / правки

- `events.html`, `event-detail.html` — новые страницы в корне.
- `css/sections.css` — новые секции (`.page-hero--media`, `.event-hero`, `.event-about`, `.program`, `.seminar-topics`, `.event-partners`, `.trust`) + reuse-блоки не дублировать.
- `css/components.css` — не менять (всё reuse из реестра).
- `js/main.js` — только если нужен аккордеон FAQ (уже есть `.faq` handler?) и «Принять участие»-вариант. Проверить существующий FAQ-хендлер.
- `assets/img/` — картинки карточек событий (webinar-*.png уже есть с главной; проверить соответствие).

## Данные секций (из аудита)

### Listing hero (node `895:9292`)
- Breadcrumb: «Главная» (16/400, `#222222` a.5) › «Медиацентр» (16/400, a1).
- h1 «Медиацентр» 890×73, 100/500/Inter Variable, `#222222`.
- Subtitle 652×44, 16/400/Inter Display: «Подпишитесь на наши новости, чтобы быть в курсе ближайших мероприятий и полезных материалов.»
- Кнопка 256×64 r50: «Подписаться на рассылку» 16/600. Справа (x+1700): Rutube 40×40 + VK 40×40, y align по кнопке.
- Header INSTANCE — идентичен главной (nav: Отрасли/Решения/Проекты/Клиенты/О нас/Мероприятия/Блог, контакты, поиск, «Оставить заявку»).

### Listing события
- 2 wide-карточки 890×560, IMAGE, r20, оверлей 450×142 (r10): title 20/500, тег-пилюля (12/400, border `#222`), дата 12/400 a.7, стрелка.
- Tabs: «Все» (актив, bg `#1342f3`), «Мероприятия», «Статьи», «Новости» — 64h, r50, 16/500.
- 6 карточек 460×560 IMAGE r20/10, оверлей 440×140/142: title 20/500, тег (Вебинар/Экспертный материал), дата «25 сентября 2025», стрелка. Тайтлы: «Почему цифровизация не работает без пересборки процесса», «Что учесть перед внедрением ЭДО: типовые ошибки и слабые места», «Заказчики «СофтСноу» представили свои проекты на Directum Awards 2026».
- «Показать ещё» 460×64 r50, 16/500, `#222` (outline-стиль).

### Detail hero (node `896:9498`)
- Frame 2094: bg `#faf9fa`, IMAGE Frame 2238 (1870×758) — фоновое фото, оверлей.
- h1 «Стратегический рывок: как настроить системную работу над будущим университета» 100/500, 1351px ширина, y≈570.
- Кнопка «Зарегистрироваться» 216×64 r50 (bg `#1342f3`, text 16/600 white).
- Под hero — Line 50 (горизонт.) + Line 51 (вертик.), ниже 2 текста: «Онлайн-семинар для руководителей» / «Современные инструменты для лидеров, реализующих стратегию на практике» (16/500, `#222` a.7).

### Программа (node `896:9608`)
- 4 пункта: «Возможности Directum Targets» (1), «Управление стратегией в вузе» (2, активный `#1342f3` bg, white text), «Демонстрация решения» (3), «Вопросы и ответы» (4). Круг 64×64 r100 (белый / `#1342f3`), цифра 20/500.
- Правый блок IMAGE 925×560 r20: label «Блок 1» (16/400 white на тёмном), title 40/500 «Управление стратегией в вузе с помощью современных инструментов», text 16/400, спикер: фото 130×130 r100 IMAGE + «Пяткин Никита» 20/500 + «Менеджер решения Directum» 16/400 + «Спикер» 14/400.

### «Что разберём» (node `896:9564`)
6 карточек 590×360 r20 (2 ряда × 3): title 40/500, text 16/400 a.7, индекс `/ 01`…`/ 06` 14/400 a.7 сверху справа. Темы: Цифровизация процессов, Единая аналитика, Контроль исполнения, Управление изменениями, Low-code платформа, Безопасная экосистема.

### Партнёры (node `896:9643`)
3 карточки 617×510 r20: name 40/500, category 14/400 a.7, desc 16/400, лого 130×130 (Directum: bg `#ff8600` r65 + белый вектор; Битрикс24: IMAGE на `#1342f3` карточке, white text; Univer Online: IMAGE на white), стрелка.

### «Почему нам доверяют ВУЗЫ?» (node `896:9667`)
- title 70/500, sub 16/400 (930px).
- 3 блока: иконка 40×40 + title 40/500 + text 16/400: «Более 30 проектов в вузах» / «Отечественный стек» / «Сертифицированный партнёр Directum».

## Отложено (не в этом этапе)

- Фильтрация по табам listing («Все/Мероприятия/Статьи/Новости») и «Показать ещё» — статично (как в clients/projects).
- Активный пункт программы и переключение «Блок 1..4» — статично (как `features` в решении, но в макете detail нет интерактива).
- Responsive/mobile — отдельная задача.

## Допуски и процедура

По `docs/FIGMA_WORKFLOW.md`: измерения relative к фрейму секции, ±1px, Figma ≠ DOM 1:1 (только визуал). Verification Isolation: НЕ трогать несвязанные секции/файлы. Модель без vision — скриншоты фиксируются, но визуальную сверку делать субагентам по дампам/числам; финальный визуальный QA — человеком или агентом с vision.

## Итоги секций (заполняется субагентами)

- [x] `events.html` — hero + события + tabs/сетка + CTA + footer
- [x] `event-detail.html` — hero + about + audience + program + cta-blue + topics + partners + trust + clients + faq + footer

## Отклонения от макета (решения приняты)

- **Listing heroBtn**: 262×64 в коде vs 256 в макете — принято (лучше выравнивается по сетке).
- **CTA→footer зазор 80** (сайтовый) vs макет 0 — принято (как на остальных страницах).
- **detail hero title**: opacity 0.7 `#222222` (не `#1342f3`) — компромисс по токенам; `max-width 1351`, letter-spacing -0.03em (не -0.07em).
- **detail hero btn**: `.btn--outline-dark` 216×64 (пустая заливка, border `#222`, текст `#222` 600) — у макета bg `#1342f3`; выбран dark-outline (совпадает с listing-CTA-кнопкой и токенами).
- **date-num** «22 апреля 2026»: `var(--color-sea)` #216CF3 (не #1342f3) — токен-компромисс.
- **CTA detail**: bg `var(--color-sea)` (не #1342f3) — компромисс.
- **trust/partners card heights** авто (контентные) — допуск по макетным высотам.
- **`.btn--outline-dark`** добавлен в `css/components.css` (после `.btn--outline:hover`) — новый вариант shared-компонента.