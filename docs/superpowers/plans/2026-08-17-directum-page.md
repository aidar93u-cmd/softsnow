# План: страница «Шаблон Экосистемы (Директум)» (`directum.html`) (2026-08-17)

Одна страница по макету «SoftSnow Work (Copy)» (fileKey `tK0WpkPKL3pQGAyCfynr3b`), SECTION node `1060:18355`:

- **`directum.html`** — «Шаблон Экосистемы (Директум)», node `1060:18355`.

Многостраничный паттерн: все страницы в корне, пути `css/...`, `assets/img/...` без `../`. Страница — лендинг про Directum (экосистема решений Directum, партнёрство SoftSnow). Большая часть секций — reuse с главной/детальных страниц + 2 новые секции (`reasons`, `vnedrenie`, `d-partner`).

## Статус этапов

| # | Этап | Статус |
|---|------|--------|
| 0 | FIGMA_AUDIT (`1060:18355` — 9 секций + footer) | [x] |
| 1 | План (этот файл) | [x] |
| 2 | IMPLEMENT: `directum.html` | [x] |
| 3 | FIGMA_QA + скриншоты + сверка | [x] |
| 4 | Обновить `FIGMA.md` | [x] |

## Результат аудита (mapping)

| # | Figma Section | Figma node | Code | Решение |
|---|---|---|---|---|
| 1 | Header INSTANCE | `863:18238` | `.header` | reuse (копия из index) |
| 2 | Hero (Frame 2094, 1870×1017): IMAGE bg 1870×758, h1 «Directum — цифровая экосистема для управления организацией» 1021×373, btn outline «Оставить заявку», Line 50/51, 2 заметки 16/500 | `1060:18364` | `.hero` + модификатор | reuse `.event-hero`-структуры, title 1021 (модификатор `.directum-hero__title`) |
| 3 | «Экосистема решений Directum» (1870×880): head white r20 360h (title 70/500 «Экосистема решений Directum», desc 655×66, carousel-nav) + 6 карточек 460×510 r20 (name 40/500, sub 16/400, лого 130×130, desc 16/400, **БЕЗ стрелки**) | `1060:18379` | `.partners` + модификатор `.partners--eco` | reuse (главная) — карточки без стрелки |
| 4 | «Почему выбирают Directum» (1870×730 white r20): слева title 890×121 + desc 655×66; справа 4 пункта (иконка 64×64 `#1342f3` + title 40/500 + text 16/400): Экосистема / No-code·Low-code / Масштабируемость / Встроенный ИИ | `1060:18355` | `.reasons` | **NEW section** |
| 5 | CTA синий (1870×433, bg `#1342f3`): title «Расскажите о вашей задаче», text 619×40, btn 460×64 «Заказать обратный звонок» | `1060:18485` | `.cta--blue` + модификатор `.cta--blue--directum` | reuse (решение-detail) |
| 6 | «Проекты на базе экосистемы» (1870×830): head white r20 360h (title «Проекты на базе экосистемы» 70/500, sub 652×44, btn «Все проекты», carousel-nav) + 3 карты 930×460 (name 24/500, text 16/400, лого 180×180, стрелка, «2025» на 3-й) | `1060:18371` | `.projects` + модификатор `.projects--eco` | reuse (главная) — карточки те же (Президентская академия / ВОЭК / ВОЭК+2025), name 24px |
| 7 | «Варианты внедрения» (1870×691): title 890×121 + 3 карточки 617×510 r20 (name 40/500, sub 16/400, 3–4 строки 15/400 с done-иконкой 24×24, кнопка 537×64 «Оставить заявку»). Средняя «Стандартный» — СИНЯЯ `#1342f3` + бейдж «Популярный» (white stroke) + белая кнопка | `1060:18486` | `.vnedrenie` | **NEW section** |
| 8 | «SoftSnow — официальный партнёр Directum» (1870×560 white r20): слева title 713×191 + desc 655×88; справа лого Directum 571×226 | `1060:18512` | `.d-partner` | **NEW section** |
| 9 | CTA светлый (1870×560, bg `#f5f5f5` + image): title «Подберём решение под ваш контур?» 761×121, text 597×60, btn 460×64 «Связаться» | `1060:18596` | `.cta` | reuse (главная) |
| 10 | Footer (1870×520) | `1060:18523` | `.footer` | reuse (копия) |

## Новые файлы / правки

- `directum.html` — новая страница в корне.
- `css/sections.css` — новые секции `.reasons`, `.vnedrenie`, `.d-partner` + модификаторы `.partners--eco`, `.projects--eco`, `.cta--blue--directum`, `.directum-hero__title`.
- `css/components.css` — НЕ менять (кнопки/carousel-nav из реестра; белая кнопка на синей карточке внедрения — `.btn--white` как section-модификатор или reuse `.btn--outline-white`, решить при имплементации).
- `js/main.js` — добавить `initSwiper` + `bindNav` для `.partners--eco` (swiper экосистемы). Accordion не нужен.
- `assets/img/` — экспорт из Figma (плейсхолдеры на время QA): логотипы 6 продуктов экосистемы (СЭД+, HR Pro, Projects, Targets, ESM, Omni — 130×130), лого Directum 571×226 (партнёрская секция), bg hero (1870×758), bg CTA светлый.

## Данные секций (из аудита)

### Hero (node `1060:18364`)
- IMAGE bg 1870×758 (фото Directum), h1 «Directum — цифровая экосистема для управления организацией» 1021×373 (`#222222`, 100/500), btn outline «Оставить заявку», Line 50 (гориз., 40,903, 1790×0) + Line 51 (вертик., 935,1017, 114×0), внизу 2 текста 16/500: «Единый цифровой контур для ключевых процессов организации» (309×34, x40) / «Входит в реестр отечественного ПО» (340×34, x975).
- СТРУКТУРА = `.event-hero` (event-detail.html:83–94): `.hero-block-inner` + `.hero__body` (`.hero__title` max-width → 1021 через модификатор `.directum-hero__title`) + btn `.btn--outline` + `.hero__footer` (2 `.hero__note`). Line 51 → вертикальный разделитель уже есть у `.hero__footer::after`.

### Экосистема (node `1060:18379`) — `.partners--eco`
- head: title «Экосистема решений Directum» 70/500, desc «Решения Directum можно внедрять поэтапно под текущие задачи, сохраняя единую архитектуру и прозрачные связи между документами, задачами, сервисами и показателями.» 16/400 655×66, carousel-nav справа.
- 6 карточек 460×510 r20 white (gap 10, карусель): name 40/500, sub 16/400 (316×34, 2 строки), лого 130×130 IMAGE по центру (x165 y190), desc 16/400 (310×66, 3 строки), БЕЗ стрелки.
- Карточки: СЭД+ / HR Pro / Projects / Targets / ESM / Omni + логотипы (IMG, отсутствуют).
- Модификаторы: `.partners--eco .partners__arrow { display:none }`, tag→16px opacity 1, лого margin-top 60, карточки `<article>` (не ссылка).

### Почему выбирают (node `1060:18355`) — NEW `.reasons`
- white r20, 1870×730. Слева (x40): title «Почему выбирают Directum» 70/500 890×121, desc «Directum — российский разработчик платформы для цифрового управления организацией…» 16/400 655×66. Справа (x940, 930×650): 4 пункта.
- Пункты: иконка 64×64 `#1342f3` (центр 40×40) + title 40/500 + text 16/400 (601×44/66):
  1. **Экосистема** (спасательный круг: 2 концентр. окружности + 4 спицы) — «Все процессы в одном цифровом пространстве. Связи между документами, задачами и показателями прозрачны.»
  2. **No-code / Low-code** (код `M10 20 L0 10 L10 0 M23.33 0 L33.33 10 L23.33 20`) — «Быстро адаптируется под меняющиеся процессы без тяжёлых доработок и с меньшей зависимостью от сложной разработки.»
  3. **Масштабируемость** (4 квадрата 10×10 rx1.67 в сетке) — «Микросервисная архитектура, открытый API и совместимость с отечественными ОС, СУБД позволяют встраивать платформу в крупный и развивающийся IT-контур.»
  4. **Встроенный ИИ** (лампочка: колба + ножка + искра) — «В экосистему уже включены готовые ИИ-помощники и сервисы для работы с документами и задачами, которые можно адаптировать под требования пользователей.»
- Иконки: inline-SVG 64×64, stroke `--color-sea` (в макете `#1342F3`), stroke-width 2, linecap round.

### CTA синий (node `1060:18485`) — `.cta--blue--directum`
- 1870×433, bg `#1342f3`. title «Расскажите о вашей задаче» 70/500 white (721×121), text 14/400 white (619×40), btn 460×64 r50 «Заказать обратный звонок» (outline white).
- = `.cta--blue` (event-detail:187–196). Модификатор `.cta--blue--directum` для title max-width 721 (у базового 595).

### Проекты (node `1060:18371`) — `.projects--eco`
- head: title «Проекты на базе экосистемы» 70/500, sub «Показываем, как решения Directum помогают автоматизировать ключевые процессы…» 16/400 652×44, btn «Все проекты» 262×64 outline r50, carousel-nav.
- 3 карты 930×460 r20 (name 464×29 — fontSize 24, одна строка; text 16/400 500×88/115; лого 180×180 r378; стрелка; на 3-й «2025» 14/400).
- Карточки = те же, что на главной: «Президентская академия» (project-1.png), «Вологодская Областная Энергетическая Компания» (project-2.png), «Вологодская Областная Энергетическая Компания» (project-2-small.png) + «2025» (`.projects__year`).
- Модификатор `.projects--eco .projects__name { font-size: 24px; min-height: 0 }`.

### Варианты внедрения (node `1060:18486`) — NEW `.vnedrenie`
- title «Варианты внедрения» 70/500 890×121 + 3 карточки 617×510 r20 white (gap 10).
- Карточка: name 40/500 (Базовый/Стандартный/Комплексный), sub 16/400 316×34, строки 15/400 с done-иконкой 24×24 слева, кнопка 537×64 r50 «Оставить заявку» (text 122×16 по центру).
- Строки:
  - Базовый: «Срок: 2–3 месяца» / «10 часов поддержки» / «4 часов обучения»
  - Стандартный: «Срок: 4–6 месяцев» / «20 часов поддержки» / «8 часов обучения»
  - Комплексный: «Срок: от 6 месяцев» / «Сложная архитектура» / «Миграция данных» / «16 часов обучения»
- Средняя карточка «Стандартный» — СИНЯЯ `#1342f3`, white текст, бейдж «Популярный» (81×23, white stroke 1, text 9/500 white, y5 x456), кнопка БЕЛАЯ (`#ffffff` fill, текст — сверить: предположительно синий), done-иконка белая. На белых карточках done: белый круг + синяя галочка.
- done-иконка: SVG 24×24 — circle white (на белых карточках сливается, видна синяя галочка) + check path stroke.

### Партнёрская (node `1060:18512`) — NEW `.d-partner`
- white r20, 1870×560. Слева (x40): title «SoftSnow — официальный партнёр Directum» 70/500 713×191 (3 строки), desc «SoftSnow оказывает полный спектр услуг по выстраиванию цифрового взаимодействия на базе решений Directum…» 16/400 655×88. Справа (x940, 930×560): лого Directum 571×226 IMAGE (отсутствует → плейсхолдер).
- Layout: grid 2 колонки 930/930, gap 10, лого по центру правой колонки.

### CTA светлый (node `1060:18596`) — reuse `.cta`
- 1870×560, bg `#f5f5f5` r20 + IMAGE (фон-картинка). title «Подберём решение под ваш контур?» 70/500 (761×121, 2 строки), text 14/400 (597×60), btn 460×64 r50 «Связаться».
- = `.cta` (index:579–588) 1:1; btn `.btn--primary.btn--lg`. bg image 37 отсутствует → `cta-bg.png` (как на остальных страницах).

### Footer (node `1060:18523`)
- 1870×520, reuse `.footer` из index (копия 591–683).

## Иконки

- Стрелки карточек/ссылок: SVG-sprite `#icon-arrow` (17×14). Слайдеры: `#icon-arrow-left`/`#icon-arrow-right` (15×14) в `.carousel-nav__btn`.
- Иконки «Почему выбирают» (4 шт, 64×64): inline-SVG stroke `#1342F3`/`--color-sea`, stroke-width 2. Код и dash — по `vectorPaths` из макета; lifebuoy и lamp — ручная сборка по геометрии фреймов (Ellipse 45 32×32@4,4 + Ellipse 46 12×12@14,14 + Line 59–62).
- done-иконка внедрения: inline-SVG 24×24 (круг + галочка).

## Отклонения от макета (решения приняты)

- **Цвет**: в макете `#1342F3`, в токенах `--color-sea #216CF3` — компромисс как на остальных страницах (проверить при QA).
- **Отсутствующие ассеты** (плейсхолдеры, ждут экспорта): 6 логотипов продуктов экосистемы, лого Directum 571×226, bg hero, bg CTA светлый. В макете у карточек экосистемы логотипы — цветные векторные лого (СЭД+ оранжевый и т.д.) — без ассетов ставится текст-плейсхолдер в блоке 130×130.
- **Иконки lifebuoy/lamp**: нет полного `vectorPaths` (мост нестабилен) — собраны по геометрии фреймов, сверить при QA.
- **Белая кнопка** на синей карточке внедрения: `.btn--white` (модификатор/вариант) — цвет текста уточнить, по умолчанию синий `#1342F3`.

## Допуски и процедура

По `docs/FIGMA_WORKFLOW.md`: измерения relative к фрейму секции, ±1px, Figma ≠ DOM 1:1 (только визуал). Verification Isolation: НЕ трогать несвязанные секции/файлы, НЕ рефакторить существующий CSS. Модель без vision — скриншоты фиксируются, числовую сверку — по дампам; финальный визуальный QA — человеком или агентом с vision.

## Итоги секций (заполняется субагентами)

- [x] `directum.html` — hero + экосистема + почему + CTA синий + проекты + варианты внедрения + партнёрская + CTA + footer

## QA-отчёт (2026-08-17)

- Сборка: `directum.html` собрана сплайсом из `index.html` (head/header/footer/sprite) + `<main>` из `C:\Temp\opencode\directum-main.html`; title/meta заменены; UTF-8 без BOM. Прошлый файл был повреждён (42 строки, обрыв на SVG логотипа).
- Рендер (chrome-devtools, 1920px, file://): все 8 секций присутствуют, без горизонтального переполнения. Swiper `.partners__swiper` (6 слайдов) и `.projects__swiper` инициализированы. Все ассеты грузятся 200.
- Сверка по DOM: hero title 100px/1021px ширина, opacity 1; бейдж «Популярный» top 30/right 40; grid внедрения 3 колонки; синяя карточка `--color-sea`, белая кнопка sea-текстом; d-partner title 713; CTA title 70px; project name 24px. Сходится с планом.
- Консоль: ошибка «Unsafe attempt to load URL file://…» — та же на готовых страницах (events.html) — особенность file://, при деплое на HTTP отсутствует.
- Отложенный визуальный QA: мост Figma недоступен (REST 403), модель без vision — финальная визуальная сверка с макетом человеком или агентом с vision. См. «Отклонения».
