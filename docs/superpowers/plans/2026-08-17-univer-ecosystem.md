# План: страница «Экосистема Univer Online» (`univer-online.html`) (2026-08-17)

Одна страница по макету «SoftSnow Work (Copy)» (fileKey `tK0WpkPKL3pQGAyCfynr3b`), FRAME node `1065:18683` «Шаблон Экосистемы (Univer Online)» (1920×8688):

- **`univer-online.html`** — «Экосистема Univer Online», node `1065:18683`.

Многостраничный паттерн: все страницы в корне, пути `css/...`, `assets/img/...` без `../`. Страница — экосистемный лендинг Univer Online (связь: с главной partners-карточка Univer Online, сейчас `href="#"`). Кандидаты: `univer-online.html` (или `univer.html`). Большинство секций — reuse готовых компонентов с главной/detail-страниц + 4 новые секции.

## Статус этапов

| # | Этап | Статус |
|---|------|--------|
| 0 | FIGMA_AUDIT (`1065:18683` — 12 секций) | [x] |
| 1 | План (этот файл) | [x] |
| 2 | IMPLEMENT: `univer-online.html` | [ ] |
| 3 | FIGMA_QA + скриншоты + сверка | [ ] |
| 4 | Обновить `FIGMA.md` + ссылка в `index.html` | [ ] |

## Результат аудита (mapping)

| # | Figma Section | Figma node | Code | Решение |
|---|---|---|---|---|
| 1 | Header INSTANCE (nav: Отрасли/Решения/Проекты/Клиенты/Медиа/О нас/Контакты + тел. +7 (812) 320-11-30 + кнопка) | `1065:18695` | `.header` | reuse (копия) |
| 2 | Hero (Frame 2094, 1870×1017): IMAGE bg 1870×758 (экспорт `univer-hero-bg.png`), h1 «Univer Online — комплекс цифровых сервисов для вузов» 100/500, кнопка «Оставить заявку» 184×64 r50, Line 50, 2 текста снизу 16/500 («Личные кабинеты…» / «Собственная платформа SoftSnow для вузов») | `1065:18685` | `.hero` + `.event-hero` (вариант) | **reuse** `.event-hero` из event-detail.html (как в vuz-плане) |
| 3 | Реестр Минцифры (1870×460 white r20): title «Решение включено в реестр Минцифры» 40/500 + 2 текста 16/400 + IMAGE сертификат 1171×781 (`univer-registry.png`) | `1073:21210` | `.registry` | reuse (решение-detail:123–140) |
| 4 | «Основа цифровой среды вуза» (1870×641): title 70/500 + 4 карточки 460×460 (Собственное решение SoftSnow / Официальная регистрация / Отечественное ПО / Дата-центры в России; name 40/500 + desc 16/400) | `1065:18696` | `.s-audience` | reuse (решение-detail) |
| 5 | «Почему выбирают Univer Online» (1870×726 white r20): слева title 70/500 + sub 16/400 w655; справа 4 строки (круг 64×64 bg `#1342f3` r100 + белая иконка 40×40, title 40/500, desc 16/400): Единая цифровая среда / Глубокая адаптация под вуз / Для всех участников процесса / Измеримый управленческий эффект | `1065:18712` | `.s-reasons` | **NEW section** |
| 6 | CTA синий (1870×453 bg `#1342f3` r20): title «Расскажите нам о задачах вашего университета» 70/500 white, text 14/400 white, btn 460×64 r50 outline white 1px «Заказать обратный звонок» | `1065:18753` | `.cta--blue` | reuse (решение-detail) + проверить btn outline |
| 7 | «Ключевые решения внутри Univer Online» (1870×1400): head (title 70/500 + sub 16/400) + 7 карточек 460×510 r20 (name 40/500, tag 14/400, IMAGE 130×130 центрир., desc 16/400): 4 верхних (Личные кабинеты / Мобильное приложение / Цифровые сервисы / Выборный сервис) + 3 нижних (Анкетирование / LMS для ДПО / CRM для ДПО) | `1065:18759` | `.partners__card` + статическая сетка | reuse компонента карточки (как vuz/главная) в grid 4+3; новый CSS `.s-services` |
| 8 | «Проекты на базе экосистемы» (1870×830): head (title 70/500 + sub 16/400 + «Все проекты» + стрелки) + 3 карты 930×460 r20 (name 40/500, text 16/400, лого 180×180 r378, стрелка): Президентская академия / ВОЭК / ВОЭК+«2025» | `1065:18797` | `.projects` | reuse (главная) |
| 9 | «Варианты внедрения» (1870×691): title 70/500 + 3 карты 617×510 r20 (Базовый/Стандартный/Комплексный; name 40/500, sub 14/400, 4 галочки-строки 20/400, btn 537×64 r50 «Оставить заявку»); средняя — bg `#1342f3` с бейджем «Популярный» и белой кнопкой | `1065:18826` | `.s-plans` | **NEW section** |
| 10 | «SoftSnow — команда…» (1870×610): title 70/500 + 2 текста 16/400 + справа 4 скриншота телефона (589×593, 301×589, 301×589, 301×562) коллаж | `1145:13529` | `.s-team` | **NEW section** |
| 11 | CTA (1870×560 bg `#f5f5f5` + IMAGE `univer-cta-bg.png`): title «Подберём решение под задачи вашего университета» 70/500 + text 14/400 + btn 460×64 «Связаться» | `1065:18910` | `.cta` | reuse (главная:579–588) |
| 12 | Footer (1870×520) | `I1072:20402` | `.footer` | reuse (копия) |

## Переиспользование иконок

- Стрелки карточек/ссылок: SVG-sprite `#icon-arrow` (17×14). Слайдеры: `#icon-arrow-left`/`#icon-arrow-right` (15×14) в `.carousel-nav__btn`.
- Иконки «Почему выбирают» (круги 64×64): иконки Phosphor/Fluent (`ph:devices-light`, `fluent:slide-search-20-regular` и т.п.) — сложные vectorPaths, экспортированы как PNG 128×128 (`icon-reason-1..4.png`).
- Иконки-картинки «Ключевые решения» (130×130): экспорт `svc-1..7.png` (260×260).
- Галочки «Вариантов внедрения»: круг 24×24 white + галочка-вектор stroke `#1342f3` 2px — inline-SVG (один path, переиспользуется).
- Скриншоты телефонов: экспорт `phone-1..4.png`.

## Новые файлы / правки

- `univer-online.html` — новая страница в корне.
- `css/sections.css` — 4 новые секции: `.s-reasons`, `.s-services`, `.s-plans`, `.s-team`. Всё остальное — reuse.
- `css/components.css` — не менять (кнопки/carousel-nav из реестра).
- `js/main.js` — не менять (все инициализации есть: `initSwiper`, `bindNav`, `initAccordion`).
- `assets/img/` — уже экспортировано: `univer-hero-bg.png`, `univer-registry.png`, `univer-cta-bg.png`, `icon-reason-1..4.png`, `svc-1..7.png`, `phone-1..4.png`, `logo-ranepa-univer.png`, `logo-voek-univer.png`.
- `index.html` — partners-карточка Univer Online: `href="#"` → `href="univer-online.html"` (после имплементации).

## Данные секций (из аудита)

### Hero (node `1065:18685`)
- IMAGE bg 1870×758 (`1065:18686`), h1 «Univer Online — комплекс цифровых сервисов для вузов» 100/500 `#222222` (x40, w1021, 3 строки), кнопка «Оставить заявку» 184×64 r50 (x40,y903, text 16/600 x70,y927), Line 50 (гориз., x40,y1289, 1790w), внизу 2 текста 16/500: «Личные кабинеты, мобильное приложение и сервисы в единой среде» (x40) / «Собственная платформа SoftSnow для вузов» (x975).
- СТРУКТУРА = `.event-hero` (event-detail.html:83–94): `<img class="event-hero__img">` 758h, `.hero__title.event-hero__title`, `.btn.event-hero__btn` (здесь стандартная синяя кнопка `.btn--primary`, не outline), `.hero__footer.event-hero__footer` + 2 `.event-hero__note`.

### Реестр (node `1073:21210`)
- white r20, 1870×460. Слева (890×380): title «Решение включено в реестр Минцифры» 40/500, 2 текста 16/400: «Сервис официально зарегистрирован в Едином реестре российских программ для ЭВМ и баз данных Министерства цифрового развития РФ.» + «Сертификат подтверждает соответствие требованиям…» (полный текст — из дампа).
- Справа IMAGE сертификат 1171×781 (`1114:5882`, экспорт `univer-registry.png`).
- = `.registry` 1:1 (решение-detail:123–140). Только контент: texts обновить по дампу.

### Основа среды (node `1065:18696`)
- title «Основа цифровой среды вуза» 70/500.
- 4 карточки 460×460 r20 white (gap 10): name 40/500 (40,34), desc 16/400 (40,354). Карточки: «Собственное решение SoftSnow» / «Официальная регистрация» / «Отечественное ПО» / «Дата-центры в России».
- = `.s-audience` 1:1 (решение-detail). Проверить: карточки `.s-audience__card` без иконок (только title+desc) — как в макете.

### Почему выбирают (node `1065:18712`) — NEW
- white r20, 1870×726. Слева (w890): title «Почему выбирают Univer Online» 70/500, sub «…» 16/400 w655.
- Справа (w695, 4 строки y10/174/338/502, каждая 104h): круг 64×64 bg `#1342f3` r100 (иконка 40×40 PNG `icon-reason-N.png`) + блок w601: title 40/500 + desc 16/400.
- Строки: (1) «Единая цифровая среда» / «Все сервисы — в одном контуре: единый вход, общие данные и согласованные процессы»; (2) «Глубокая адаптация под вуз» / «Настраиваем сервисы под реальные процессы и регламенты вашего университета»; (3) «Для всех участников процесса» / «Студенты, преподаватели, сотрудники и администрация — у каждой роли свой сценарий»; (4) «Измеримый управленческий эффект» / «Прозрачная аналитика и цифровые сервисы, которые сокращают ручные операции» (точные тексты — из дампа).

### CTA синий (node `1065:18753`)
- 1870×453 bg `#1342f3` r20. title «Расскажите нам о задачах вашего университета» 70/500 white (940×121, центрирован по x), text 14/400 white (528×60, центрирован), btn 460×64 r50 «Заказать обратный звонок» 16/500 (OUTLINE: stroke white 1px, без заливки; text white).
- = `.cta--blue` (решение-detail:502–511), но кнопка outline — проверить: `.btn--outline` на синем фоне или новый модификатор `.cta--blue .btn--outline`.

### Ключевые решения (node `1065:18759`) — NEW CSS `.s-services`
- head: title «Ключевые решения внутри Univer Online» 70/500 + sub 16/400 (2 строки).
- 7 карточек 460×510 r20 white (как `.partners__card` из partners/главная): name 40/500 (40,36), tag 14/400 (40,136, opacity .7), IMAGE 130×130 центрир. (x165,y190), desc 16/400 (40,404).
- 4 верхних (gap 10): Личные кабинеты / Мобильное приложение / Цифровые сервисы / Выборный сервис (tags: «Для студентов, преподавателей и сотрудников» / «Все сервисы в смартфоне» / «Автоматизация ключевых процессов в вузах» / «Конкурсы и выборы онлайн»).
- 3 нижних: Анкетирование / LMS для ДПО / CRM для ДПО (tags: «Обратная связь и аналитика» / «Обучение и управление программами» / «Работа с заявками и слушателями»).
- desc — из дампа (строки 51–54, 61–63).

### Проекты (node `1065:18797`)
- head: title «Проекты на базе экосистемы» 70/500 + sub 16/400 + «Все проекты» 16/500 + 2 стрелки.
- 3 карты 930×460 r20 white: name 40/500 (40,40), text 16/400 (40,157), лого 180×180 r378 (x710,y240), стрелка `#1342f3` (x873,y40). Карточки: «Президентская академия» + текст RANEPA; «Вологодская Областная Энергетическая Компания» + «Цифровая трансформация документооборота Президентской академии…»; та же ВОЭК + «2025» fs16 opacity .6.
- = `.projects` (главная:495–545). Логотипы: `logo-ranepa-univer.png`, `logo-voek-univer.png` (экспорт). 3-я карточка «2025» — тег.

### Варианты внедрения (node `1065:18826`) — NEW CSS `.s-plans`
- title «Варианты внедрения» 70/500 (890×121).
- 3 карты 617×510 r20 (gap 10): name 40/500 (40,35), sub 14/400 (40,131), 4 строки галочек 20/400 (с 40,237, gap 44): строка = круг 24×24 white + галочка stroke `#1342f3` 2px + text; btn 537×64 r50 (40,411).
  - Базовый (white): sub «Подходит, если вузу нужно быстро внедрить конкретное решение…»; строки: Один сервис или модуль / Базовая настройка / Минимум интеграций / Быстрый запуск; btn blue `#1342f3` white text.
  - Стандартный (bg `#1342f3`, white text): бейдж «Популярный» fs12 (рядом с name), sub «Подходит вузам, которым важно объединить несколько сервисов…»; строки: Несколько сервисов / Настройка под процессы / Интеграции с системами вуза / Поэтапное развитие; btn white, text `#222`.
  - Комплексный (white): sub «Подходит для создания единой экосистемы…»; строки: Единый цифровой контур / Комплексная архитектура / Миграция и интеграции / Развитие под новые задачи; btn blue.
- Галочка: inline-SVG круг white + path галочки stroke `#1342f3` (для синей карты — круг white + stroke `#1342f3`).

### Команда (node `1145:13529`) — NEW CSS `.s-team`
- 1870×610. Слева (w890): title «SoftSnow — команда, которая создаёт и развивает Univer Online» 70/500 (645×261), 2 текста 16/400 (655×108): «SoftSnow создаёт порталы университетов „под ключ“…» + «Это значит, что университет получает не только платформу…».
- Справа (w930): коллаж 4 скриншотов телефона: `phone-1.png` 589×593, `phone-2.png`/`phone-3.png` 301×589 (один и тот же хэш `d166f3cb`), `phone-4.png` 301×562; позиции (relative x985–1525, y240–266) — лёгкий нахлёст, как в макете.

### CTA (node `1065:18910`)
- 1870×560 bg `#f5f5f5` r20 + IMAGE `univer-cta-bg.png`. title «Подберём решение под задачи вашего университета» 70/500 (940×121), text 14/400, btn 460×64 r50 «Связаться» 16/500.
- = `.cta` (главная:579–588). btn «Связаться» — `.btn--primary`.

## Отклонения от макета (решения приняты)

- **CTA-синий кнопка**: outline (stroke white 1px, без заливки) — это НЕ `.btn--primary` (заливка). Использовать `.btn--outline` с белым текстом (проверить существующий вариант; при необходимости точечный CSS в `.s-reasons`/`.cta--blue`).
- **Бейдж «Популярный»** на синей карте «Стандартный» — fs12 white, позиция inline с name (не pill-компонент, просто текст).
- **3-я проектная карточка** («2025») — дубль ВОЭК, как в макете (vuz-план также дублировал).
- **`--color-sea` vs `#1342f3`**: макет использует `#1342f3` (RGB 19,66,243). FIGMA.md: Primary `--color-sea #1342F3`. Использовать токен `--color-sea` (совпадает).
- **Курсор** (`1065:18880`, 21×23) в карте «Стандартный» — декоративный артефакт макета, НЕ рендерить.
- **Контент sub/desc** в секциях 5, 9, 10: точные тексты — из `univer-texts-abs.txt` (дамп). Если текст в макете — «Lorem», взять осмысленный в стиле проекта (как на других страницах).

## Допуски и процедура

По `docs/FIGMA_WORKFLOW.md`: измерения relative к фрейму секции, ±1px, Figma ≠ DOM 1:1 (только визуал). Verification Isolation: НЕ трогать несвязанные секции/файлы, НЕ рефакторить существующий CSS. Модель без vision — скриншоты фиксируются, числовую сверку — по дампам; финальный визуальный QA — человеком или агентом с vision.

## Итоги секций (заполняется субагентами)

- [ ] `univer-online.html` — hero + реестр + основа среды + причины + CTA синий + ключевые решения + проекты + варианты + команда + CTA + footer