# Solution detail — мобильная версия (Figma 1742:12742)

Дата: 2026-08-23. Макет: «Шаблон детальной решения» node `1742:12742` (375×6790).
Шапка и футер — уже сделаны (общие блоки), не тронуты.

## Дубли разметки (solution-detail.html)

| Дубль | PC | Мобильный |
|---|---|---|
| `.registry__stats` (новый) | скрыт (`display:none`, sections.css «Mobile-only duplicates») | статы из hero переехали в карточку реестра (макет: Stats внутри Card/Registry) |
| `.projects__cta` (существующий паттерн index) | скрыт | кнопка «Все проекты» под каруселью |
| `.events__cta` (существующий паттерн index) | скрыт | кнопка «Перейти в медиацентр» под каруселью |
| `.hero__footer--stats` | виден | скрыт (`.hero--solution .hero__footer--stats`) |

Фон hero перенесён из inline style в CSS (`.hero--solution .hero-block-inner` в
sections.css), чтобы media.css мог отключить его на мобильном (в макете hero —
светлый блок без фото).

## CSS (media.css, блок ≤768 «Solution detail»)

- **Hero**: без фото, `--bg-light`, r10, паддинг `[16,16,24]`; breadcrumb —
  только «Главная / Решения» (sep+current скрыты); sub fs14; кнопка через 48px
  после текста, 319×46.
- **Registry**: flex-колонка, порядок: бейдж (190px) → статы (num fs19/500
  синий, label fs14) → заголовок fs19 → текст fs14; фон `--bg-light` (в макете
  фон-картинка — ассета нет, см. «Расхождения»).
- **Tasks**: паддинг `[24,16]`, title fs24, sub fs14, пункты fs16, кнопка-круг
  38×38 `--bg-light` без обводки, ответ fs14 @70.
- **Audience**: карточки auto-h, паддинг 16, r12, gap 8, name fs19, desc fs14.
- **Features**: чипы h34 fs14 в горизонтальный скролл (скроллбар скрыт); панель
  в колонку: name fs19, desc fs15, points fs15, demo-кнопка full width r10
  (иконка 47×30, title fs16, sub fs12), скриншот r12 без рамки.
- **CTA blue**: паддинг `[24,16]`, r12, title fs24, текст fs12, кнопка full width.
- **Testimonials**: слайды 300px, карточка auto-h min 280, паддинг 16, текст
  fs14, carousel-nav скрыт.
- **Projects**: модификатор `.projects--solution` — заголовок 32px (в макете
  детальной страницы fs32, на главной 24 — разные, потому модификатор); кнопка
  под каруселью — общий паттерн.
- **Events**: кнопка под каруселью — общий паттерн.
- **FAQ**: паддинг `[24,16]`, r10, вопрос fs16, иконка 16px, q без min-height
  (pad 14px), ответ fs14.

## Замеры (375, chrome-devtools, изолированный контекст)

| Элемент | Figma | DOM |
|---|---|---|
| Hero кнопка | 319×46 | 319×46 |
| Registry бейдж | 190, ry24 | 190, ry24 |
| Registry статы | 319, ry119 | 319, ry111 |
| Registry title/desc | 19/14 | 19/14 |
| Tasks title | 16/24 | 16/24 |
| Tasks пункт | fs16 | fs16 |
| Tasks кнопка | 38×38 круг | 38×38 круг |
| Audience name/desc | 19/14 | 19/14 |
| Чипы | h34, fs14 | 34, 14 |
| Features name/desc | 19/15 | 19/15 |
| Demo-кнопка | 319×71 | 319×79 |
| CTA blue кнопка | 319×48 | 319×46 |
| FAQ имя/иконка | 16/16 | 16/16 |
| Горизонтальный скролл | — | нет |

Скриншот: `C:\Temp\opencode\sd-mobile-final.jpeg` (fullpage), PC-контроль:
`sd-desktop-hero.jpeg` (фон hero и скрытие дубля — без изменений).

## Расхождения (осознанные)

1. **Фон карточки реестра**: в макете — фоновая картинка (светлая графика),
   ассета в проекте нет; использован однотонный `--bg-light`. Добавить ассет —
   по запросу.
2. **Бейдж в реестре**: в макете сверху логотип SoftSnow «экосистема цифровых
   сервисов» (ассета нет); использован существующий бейдж реестра Минцифры
   (`single-logo-reestr.svg`) — сохраняет смысл блока.
3. **Стрелка в списке задач**: в макете глиф 17×10; использован существующий
   спрайт `#icon-arrow` 17×14 (правило спрайтов AGENTS.md).
